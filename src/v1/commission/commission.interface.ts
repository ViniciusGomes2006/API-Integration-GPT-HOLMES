export interface propertiesType {
  closed_deal: boolean;
  registration_goal: boolean;
  photo_goal: boolean;
  productivity_goal: boolean;
  admin_tax: number;
  property_registered: boolean;
  total_commission: number;
}